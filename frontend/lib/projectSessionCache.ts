/**
 * Project Session Cache utilities
 * Stub implementation to fix build
 */

export function getCachedProjectSession(agentId: number) {
  return null
}

export function setCachedProjectSession(agentId: number, session: any) {
  // No-op stub
}

export function clearCachedProjectSession(agentId: number) {
  // No-op stub
}
