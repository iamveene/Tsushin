# ============================================================================
# Tsushin Docker Environment — Minimal Template
#
# System-level configuration only. Integration API keys (AI providers, tools)
# are managed via the UI and stored encrypted in the database.
#
# For the full reference, see env.example.
# The installer (python3 install.py) generates .env automatically.
# ============================================================================

# Application
TSN_APP_HOST=0.0.0.0
TSN_APP_PORT=8081
TSN_BACKEND_URL=http://localhost:8081
TSN_FRONTEND_URL=http://localhost:3030

# PostgreSQL (REQUIRED)
# Generate: python3 -c "import secrets; print(secrets.token_urlsafe(24))"
POSTGRES_PASSWORD=

# Security (REQUIRED)
# Generate: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=

# Hub OAuth encryption
# Generate: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ASANA_ENCRYPTION_KEY=

# Logging
TSN_LOG_LEVEL=INFO

# Docker-in-Docker: Host path for MCP container volume mounts (REQUIRED)
HOST_BACKEND_DATA_PATH=

# SSL/HTTPS (Caddy)
SSL_MODE=disabled

# Frontend
NEXT_PUBLIC_API_URL=http://localhost:8081

# Container Runtime
TSN_CONTAINER_RUNTIME=docker
TSN_SECRET_BACKEND=env
