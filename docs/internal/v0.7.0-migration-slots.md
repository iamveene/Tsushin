# v0.7.0 Migration Slots

This allocation prevents parallel track collisions after Phase 0 exits. Tenant
foreign keys must use `String(50)` because `tenant.id` is a string primary key.

| Revision | Owner | Purpose |
| --- | --- | --- |
| `0045` | Phase 0 | `message_queue.message_type`, `channel_event_dedupe`, Sentinel `continuous_agent_action_approval` toggles |
| `0046` | Track A | Default-agent FKs and `user_channel_default_agent` |
| `0047` | Track A | Continuous-agent control-plane models |
| `0048` | Track D | Whisper ASR instances |
| `0049` | Track F | `agent_skill` tool-result toggles and `conversation_thread.agentic_scratchpad` |
| `0050` | Track A | `channel_event_rule` |
| `0051` | Track A | Email trigger instance |
| `0052` | Track B | Jira trigger instance; defer until after the no-migration dispatch foundation |
| `0053` | Track B | Schedule trigger instance; defer until after the no-migration dispatch foundation |
| `0054` | Track B | GitHub trigger instance; defer until after the no-migration dispatch foundation |
| `0055` | Track E | Audit log hardening |
| `0056` | Reserved | Unused in v0.7.0; Stripe/Billing is deferred to v0.7.x |
| `0057` | Track F | Platform agentic-loop bounds on `Config` |
| `0058` | Track F | Agent-level agentic-loop bounds |
| `0059` | Track D | Tenant default ASR instance; already landed before Track A2/Track F continuation |

Rules:

- Do not create `track/*` worktrees until Phase 0 is merged and validated.
- Do not reuse or renumber `0045`; it is the serial Phase 0 foundation.
- Track F must not modify `continuous_run`; Track A creates that table in `0047`.
- Every migration must include downgrade handling and idempotency checks where partial-state recovery is plausible.
- The current release chain is linear and intentionally out of numeric order:
  `0044 -> 0045 -> 0046 -> 0048 -> 0051 -> 0059 -> 0047 -> 0050 -> 0049 -> 0057 -> 0058`.
- The Track B dispatch foundation is a no-migration slice. When adapter tables are added later, chain them from current head as `0052 -> 0053 -> 0054`, with `0052.down_revision = "0058"`.
