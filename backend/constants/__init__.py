"""Backend constants — single-source-of-truth enums / literal sets.

This package exists to hold named constants that were historically spelled
out inline in multiple routes / schemas / models. Keeping them in one place
prevents drift when new values are added.
"""
