"""Embedding provider adapters for the case-memory subsystem.

Each module in this package exposes one concrete ``EmbeddingProvider``
implementation (the ABC lives in ``agent.memory.embedding_service``).
"""
